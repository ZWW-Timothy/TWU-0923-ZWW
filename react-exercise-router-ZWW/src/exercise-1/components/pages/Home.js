import React from "react"

const Home = (props) => {
    return (
        <div>
            <h1>This is a beautiful Home Page</h1>
            <h1>And the url is '/'.</h1>
        </div>

    );
};

export default Home;
